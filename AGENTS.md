# 给 AI Agent 的操作说明

你正在帮用户清理 macOS 上微信占用的磁盘空间。这个仓库里的 `wechat_cleaner.py`
是工具，你负责替用户看懂扫描结果、问清楚他想放哪、然后代他执行。

## 背景：这个问题是什么

macOS 微信会把聊天里收到的**文件和视频**永久存在本地，按 `年-月` 目录堆积，
**从不自动回收**。重度用户一年能攒几十 GB。用户通常完全不知道它们在哪。

路径长这样（`wxid_xxx` 是用户的微信号目录，每台机器不同）：

```
~/Library/Containers/com.tencent.xinWeChat/Data/Documents/xwechat_files/wxid_xxx/msg/
├── file/2025-08/  2025-09/  …   ← 聊天收到的文件
└── video/2025-08/ 2025-09/  …   ← 聊天收到的视频
```

## 三步流程

### 第 1 步：扫描（绝对安全，不碰任何文件）

```bash
python3 wechat_cleaner.py --json
```

返回 JSON：

```json
{
  "version": "1.0",
  "keep_months": 3,
  "accounts": [{
    "account_dir": "/Users/xxx/Library/.../wxid_xxx",
    "kinds": {
      "video": {"total_bytes": 24696061952, "total_files": 1893,
                "months": [{"month": "2025-11", "bytes": 4187593114,
                            "files": 213, "expired": true}]},
      "file":  {"total_bytes": 12884901888, "total_files": 1247, "months": [...]}
    },
    "expired_bytes": 34359738368,
    "expired_human": "32.0GB"
  }],
  "suggested_dest": "/Users/xxx/Desktop/微信文件整理"
}
```

`expired: true` = 超过保留窗口、可以搬走的月份。

**把结果讲成人话给用户听**，例如：
> 你微信存了 34 GB 的聊天文件，其中视频 23 GB、文档 11 GB。
> 最占地方的是 2025-11（3.9 GB）。三个月以前的一共 32 GB 可以清出来。

### 第 2 步：问用户两件事

1. **搬到哪里？** 建议用 `suggested_dest`，但要让用户确认或改。
2. **保留最近几个月？** 默认 3。想更保守就 6，想更狠就 1。

**不要替用户决定这两件事。** 位置错了他会找不到，窗口错了可能搬走他还要用的。

### 第 3 步：执行

```bash
python3 wechat_cleaner.py --dest "/Users/xxx/Desktop/微信文件整理" --months 3 --yes
```

`--yes` 表示"你已代用户确认过"。**没有真的问过用户就不要加这个参数。**

执行后：
- 文件按类型分好（📄文档 / 🎬视频 / 🖼图片 / 📊表格 / 📦压缩包 …）
- 目标目录里生成 `_说明.txt`
- 微信那边只留最近 N 个月

## 必须告诉用户的三件事

1. **删掉这些文件不会损坏聊天记录。** 聊天里的文字一条都不会少，只是以后点那个
   文件会显示"已过期或已被清理"。而且这些文件在腾讯服务器上通常也早就过期了，
   本来就点不开。
2. **这个工具只移动，不删除。** 删不删、删哪些，全部由用户自己决定。
3. **`attach` 目录（聊天图片）不在处理范围内。** 那个删了聊天记录里的图会大面积
   变灰，风险明显更高，本工具刻意不碰。

## 可选：装定期任务

用户如果想一劳永逸，可以装一个 launchd 定时任务，每 N 个月自动跑一次，
跑完在「提醒事项」建一条待办。交互模式最后会问；你也可以直接告诉用户
运行 `python3 wechat_cleaner.py` 走一遍问答。

## 边界：不要做的事

- ❌ **不要自己写 `rm` 去删微信目录。** 用这个工具，它只移动、有日志。
- ❌ **不要碰 `attach/`（图片）和 `db_storage/`（聊天数据库）。**
  数据库是聊天记录本体，动了会真的丢数据。
- ❌ **不要碰 `Backup/` 目录。** 那是用户主动做的"聊天记录备份到电脑"，
  可能几十 GB，但也可能是他唯一的备份。**发现了要单独提醒用户，让他自己决定**，
  绝不自动处理。
- ❌ **没问过用户就不要加 `--yes`。**

## 一个真实案例

2026-08，一位用户的微信占了 **154 GB**：

| 组成 | 大小 | 处理 |
|---|---|---|
| `Backup/`（2024-12 做的聊天记录备份） | 89 GB | 先上传网盘，核对完整后才删 |
| `Backup/`（2025-08 又做了一次） | 8.9 GB | 确认无用后删 |
| `msg/video` + `msg/file` | 35 GB | 本工具处理 |
| `msg/attach`（图片） | 11 GB | 不动 |

清完剩 19 GB。**最大的一块是用户自己点"备份聊天记录到电脑"生成的，
他早就忘了做过这事** —— 所以扫描时发现 `Backup/` 一定要问，不要假设它没用。
