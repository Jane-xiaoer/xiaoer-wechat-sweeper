# 更新记录

## 2.5.0 · 2026-08-16

**面板记住你的选择了 —— 三样都记。**

2.4.2 记住了搬迁文件夹，但面板上另外两样你会调的东西
每次打开还是弹回出厂值：「留几个月」的滑杆，和「先查重」的勾选。
这版一并记住。

比如你习惯留 6 个月、不查重，下次打开面板就是这个样子，
不用再拖一遍、点一遍。

填回控件发生在第一次扫描之前 —— 否则你会看到滑杆先跳一下、
数字跟着变一次，像是工具自己改了主意。

「一个月都不留」是个合法选择，也会被记住。配置文件要是被手改坏了，
安静回到出厂值，面板不会因此显示成乱的。

（这些记录跟文件夹一样存在 app 外面，自动更新冲不掉。）

## 2.4.2 · 2026-08-16

**修掉 macOS 上的双击闪退。** 有人反馈双击没反应，查下来是从第一版
就埋着的坑，跟自动更新无关。

启动壳跑的 `/usr/bin/python3` 根本不是 Python，只是 xcode-select 的
转发壳，真身在 Command Line Tools 里 —— 没装过 Xcode 的电脑上它跑不起来。
而启动命令把错误全丢进了 `/dev/null`，用户零反馈，只看到 Dock 图标
闪一下就没了。

现在会按顺序找一台电脑上真能跑的 Python（Homebrew、python.org、
命令行工具都认），一个都没有就弹窗说清楚，并直接把下载页打开。
启动日志也不再丢弃，写进 `~/Library/Logs/小耳微信清扫器.log`。

**记住你上次选的文件夹。** 以前每次打开都从「桌面/微信文件整理」开始，
你上次搬到哪儿了工具完全不记得。现在会记住，下次打开默认还用它。
那个文件夹要是被删了或者在拔掉的外接盘上，就安静回到默认位置。

（这个记录存在 app 外面，所以自动更新不会把它冲掉。）

**打开变快了。** 2.4.0 把「查有没有新版」放在了开面板之前，网络不通的时候
要等三秒多才有反应 —— 而那几秒里 Dock 图标已经消失了，看着就像闪退。
现在查更新挪到后台，实测断网启动从 3.13 秒降到 0.11 秒。

## 2.4.1 · 2026-08-16

**把换包改成原子的。** 2.4.0 的换包顺序有个几秒钟的空窗：

先把旧 app 挪成 `.old`，再花几秒把新的复制进来 —— 这几秒里磁盘上
根本没有 app。进程要是在这时候死掉（关窗口、睡眠、断电），
你的图标就没了。更要命的是那个「下次启动自动救回」的兜底有死锁：
图标都没了，你根本没法启动它，兜底永远等不到触发的机会。

新顺序把慢的那步放在旧 app 还完好的时候做完：先把新 app 复制到
旁边，再用两次改名完成切换 —— 同一个盘上改名是原子的。
危险窗口从几秒缩到微秒，而且崩在任何一步，磁盘上都留着一份
完整能跑的 app。

## 2.4.0 · 2026-08-16

**打开就自动更新了。**

以前发了新版，你手上那份不会知道——只能自己回 Release 页重新下一次。
现在双击打开时它会先问一句 GitHub，有新版就直接装好再进面板，
不用点任何按钮。查不到、超时、装不成，一律照常用现在这版，
绝不会因为更新失败而打不开。

macOS 上是整包替换：app 是签名公证过的，改里面任何一个文件
都会让签名失效、被系统判定成损坏，所以只能换整个 app。
换之前要过三关——包的 sha256、签名与 TeamID、公证票据。
第二关最要紧：签名有效只证明「某个 Apple 开发者签了它」，
不证明是我们，所以要认准 TeamID。三关有一关不过就一个字节都不写进去。
替换时先把旧 app 挪成 `.old` 再让新的就位，万一中途断电，
下次打开会自动拿它救回来。

Windows 上没有签名，走目录覆盖，同样先验 sha256。
装在没有写权限的地方（比如 Program Files）就静默跳过，不要管理员权限。

## 2.3.0 · 2026-08-16

**加了 Windows 版。** 核心逻辑（扫描、分类、查重、搬运）两个系统通用，
差异都收在一层适配里：

- Windows 上微信数据目录可以被搬到任意盘，工具会去
  `%APPDATA%\Tencent\xwechat\config\*.ini` 读微信记录的真实位置，读不到再退回默认位置。
- 同时支持两代目录布局：新版 `xwechat_files\<wxid>\msg\{file,video}\YYYY-MM`
  和旧版 `WeChat Files\<wxid>\FileStorage\{File,Video}\YYYY-MM`。
- 选文件夹走 PowerShell 的 FolderBrowserDialog，打开文件夹走 explorer，
  Time Machine 快照检查在 Windows 上跳过。
- 控制台代码页与 ANSI 转义一并处理，中文不再是方块。
- 双击入口：`小耳微信清扫器.bat`（Windows 暂未打包 exe，用源码跑）。

**只留面板这一条路，第一版的东西清干净了。**

工具早就是「双击 → 浏览器弹出面板 → 点几下」，但仓库里还留着第一版的三条老路，
说明也还在讲它们。这版一并清掉：

- 删掉终端版：入口 `小耳微信清扫器.command` 与 `wechat_cleaner.py` 里那套
  一问一答的界面代码（`--json` / `--yes` / `--check-space` 等参数随之取消）。
  该文件从 768 行瘦到 360 行，只剩面板真正调用的 11 个函数，面板行为一字未变。
- 删掉 `AGENTS.md` 与 README 里「交给你的 AI 助手」一节。
- README 与两张推广卡片里所有讲终端、AI、`⚠️ 待清理_日期/`、`✅ 我要留的/`
  的段落全部清掉 —— 后两样面板从来就不生成，属于第一版遗留的描述。

**面板里的分类名也繁体了。**

界面早就是繁体，但跑完结果页显示的「🎬 视频」「📄 文档/💼 简历」还是简体 ——
那些名字由后端算出来经接口送到前端，绕开了只作用于界面文件的转换。
现在接口回的分类名与进度文案统一走同一张转换表。

**硬盘上的文件夹名保持简体不变**，免得已经清扫过的人目录里又多出一套繁体夹子；
真实路径与报错信息一律不转（报错里常带路径，转了会误导人照着去找）。

**文档纠正**：此前描述的「每批单独建带日期的文件夹」与代码不符 ——
实际是并进同一套分类夹，同名自动加序号（`xxx_1.pdf`）不覆盖，靠文件原始时间分新旧。

## 2.2.1 · 2026-08-15

- 补齐繁体转换表：「留著嗎」「點開」「沒有」等 46 个字此前仍显示为简体。
- 「乾淨」改走词组转换，此前逐字转会得到错字。
- 移除命令行版的定时任务功能：不再询问是否设置定期自动整理，
  也不再安装 launchd 任务。工具的用法就是你想起来时自己打开一次。
- 文档同步：说明没有定时任务、不常驻后台、不开机自启；
  第二次清扫仍归入同一个目标文件夹。

## 2.2.0 · 2026-08-15

- 全面启用粉绿水彩与宣纸质感的图形界面。
- 更新为水彩耳朵与红扫帚应用图标。
- 新增毛笔式月份滑杆和明确的“請您選擇”提示。
- 页面切换增加湿墨晕染效果，常态加入克制的漂浮动效。
- 优化分隔线、数据区、文件夹区域及繁体中文排版。
- 尊重 macOS“减少动态效果”辅助功能设置。

## 2.1.4 · 2026-08-15

- 发布绿色剪纸耳内扫帚图标。
